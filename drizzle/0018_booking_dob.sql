-- Дата народження пацієнта у заявці. Потрібна як код входу в кабінет
-- пацієнта (телефон + дата народження) і як ідентифікатор особи для персоналу.
ALTER TABLE `bookings` ADD COLUMN `date_of_birth` text NOT NULL DEFAULT '';
