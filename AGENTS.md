# AGENTS.md — RadiologyOS

Цей файл є обов'язковою інструкцією для Codex, ChatGPT та інших AI-агентів, які змінюють код RadiologyOS.

## Перед будь-якою зміною

1. Прочитати:
   - `docs/business-core.md`
   - `docs/design-system.md`
   - `docs/components.md`
   - `docs/pages.md`
   - `docs/domain-model.md`
   - `docs/roles-permissions.md`
   - `docs/workflows.md`
   - `docs/lessons.md`

2. Перевірити, чи вже існує потрібний компонент або сценарій.
3. Не створювати дублікати компонентів, сторінок, ролей або модальностей.
4. Не змінювати погоджену термінологію без окремого рішення власника продукту.
5. Для будь-якої внутрішньої бізнес-функції спочатку визначити її місце в канонічному ланцюжку `Довідник → Документ → Проведення → Регістр → Звіт` і не обходити ядро окремим ad-hoc станом без обґрунтування.

## Обов'язкові правила

- Основна мова інтерфейсу — українська.
- Поточний етап — Web MVP.
- Локальний сервер, PostgreSQL, Orthanc і DICOM — наступний етап.
- Не зберігати реальні медичні дані на GitHub Pages або в `localStorage`.
- Не додавати роль «старша медична сестра».
- Є роль «старший рентгенлаборант».
- Використовувати «пантомографія», не «мамографія».
- Погоджені модальності: КТ, рентгенографія, флюорографія, пантомографія, УЗД.
- Одна заявка належить одному пацієнту і може містити кілька досліджень.
- Календар організовується за кабінетами/обладнанням.
- Внутрішні сторінки повинні мати `noindex,nofollow`.
- AI не приймає клінічне рішення замість лікаря.
- Внутрішня система `/staff` є BAS Small Company-подібною операційною системою клініки; публічний сайт є окремою вітриною.
- Проведені господарські документи не можна тихо переписувати: зміни фактів виконуються через коригування/сторно та простежуваний зв'язок із першоджерелом.
- Підсумки на кшталт грошей, боргу, залишків, виробітку та навантаження походять із документів/рухів, а не з вручну редагованих агрегованих полів, якщо немає окремо задокументованої причини.
- Друковані форми є частиною ядра: шаблони версіонуються, а сформовані історичні форми мають бути відтворюваними з конкретного snapshot.

## Після зміни

1. Оновити `docs/changelog.md`.
2. Якщо з'явилося нове правило або висновок — оновити `docs/lessons.md`.
3. Якщо створено нову сторінку — оновити `docs/pages.md`.
4. Якщо створено новий компонент — оновити `docs/components.md`.
5. Якщо змінено сутність або поля — оновити `docs/domain-model.md`.
6. Якщо змінено права — оновити `docs/roles-permissions.md`.
7. Якщо змінено бізнесовий життєвий цикл, проведення, регістри або друковані форми — оновити `docs/business-core.md`.

## Стиль змін

- Маленькі, зрозумілі коміти.
- Одна функціональна задача — один PR.
- Не переписувати весь проєкт без потреби.
- Зберігати зворотну сумісність із поточним MVP, якщо це можливо.
- Не змінювати `main` напряму для великих змін.