"use client";
import {useEffect,useState} from "react";
type P={templateVersion:number;formType:"service_act";organization:{name:string};document:{id:number;number:string;occurredAt:string;state:string;comment:string;createdBy:string;createdAt:string;postedBy:string;postedAt:string};booking:{id:number;code:string;patientName:string;patientId:string;patientCategory:string};service:{code:string;title:string;equipmentId:string;durationMinutes:number;anatomicalRegionsCount:number;performedAt:string;radiologistEmail:string;radiographerEmail:string;priceAmount:number;chargeAmount:number;currency:string}};type S={id:number;documentId:number;formType:string;templateVersion:number;documentState:string;generatedBy:string;generatedAt:string;storageKey:string;sha256:string};type R={snapshot:S;payload:P;error?:string};
const ST:Record<string,string>={posted:"Проведено",reversed:"СТОРНО",draft:"ЧЕРНЕТКА",cancelled:"Скасовано"};const money=(v:number,c="UAH")=>new Intl.NumberFormat("uk-UA",{style:"currency",currency:c,maximumFractionDigits:0}).format(Number(v||0));const fd=(v:string)=>{const d=new Date(v);return Number.isNaN(d.getTime())?v:d.toLocaleString("uk-UA",{dateStyle:"medium",timeStyle:"short"});};
export default function ServiceActPrintPage(){const[data,setData]=useState<R|null>(null),[error,setError]=useState("");useEffect(()=>{const c=new AbortController(),timer=window.setTimeout(()=>{const id=Number(new URLSearchParams(window.location.search).get("id"));if(!Number.isInteger(id)||id<1){setError("Некоректний документ надання послуги");return;}void fetch("/api/staff/service-deliveries/print",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({documentId:id}),signal:c.signal}).then(async r=>{const p=await r.json().catch(()=>({})) as R;if(!r.ok)throw new Error(p.error||"Не вдалося сформувати акт");setData(p)}).catch(e=>{if(e?.name!=="AbortError")setError(e instanceof Error?e.message:"Помилка друку")});},0);return()=>{clearTimeout(timer);c.abort()}},[]);if(error)return <main className="financePrintPage"><div className="financePrintSheet"><h1>Не вдалося сформувати акт</h1><p>{error}</p></div></main>;if(!data)return <main className="financePrintPage"><div className="financePrintSheet"><p>Формування акта наданих послуг…</p></div></main>;const{payload:p,snapshot:s}=data,paid=p.service.chargeAmount>0;return <main className="financePrintPage"><div className="financePrintToolbar"><button onClick={()=>window.close()}>Закрити</button><button onClick={()=>window.open(`/api/staff/printed-forms/pdf?snapshotId=${s.id}`,"_blank","noopener,noreferrer")}>Завантажити PDF</button><button onClick={()=>window.print()}>Друкувати</button></div><article className="financePrintSheet"><header><div><small>{p.organization.name}</small><h1>Акт наданих послуг</h1><p>Документ {p.document.number}</p></div><strong>{ST[p.document.state]||p.document.state}</strong></header><section className="financePrintMeta"><div><span>Номер</span><b>{p.document.number}</b></div><div><span>Дата документа</span><b>{fd(p.document.occurredAt)}</b></div><div><span>Заявка</span><b>{p.booking.code}</b></div><div><span>Пацієнт</span><b>{p.booking.patientName}</b></div><div><span>Категорія</span><b>{p.booking.patientCategory==="military"?"Військовослужбовець":"Цивільний"}</b></div><div><span>Виконано</span><b>{fd(p.service.performedAt)}</b></div></section><section className="financePrintDetails"><p><span>Послуга</span><b>{p.service.title}</b></p><p><span>Код послуги</span><b>{p.service.code}</b></p><p><span>Апарат / кабінет</span><b>{p.service.equipmentId}</b></p><p><span>Тривалість</span><b>{p.service.durationMinutes} хв</b></p><p><span>Анатомічних ділянок</span><b>{p.service.anatomicalRegionsCount}</b></p><p><span>Лікар</span><b>{p.service.radiologistEmail||"—"}</b></p><p><span>Рентгенолаборант</span><b>{p.service.radiographerEmail||"—"}</b></p></section><section className="financePrintAmount"><span>{paid?"Нараховано":"Умови надання"}</span><b>{paid?money(p.service.chargeAmount,p.service.currency):"Безоплатно"}</b></section>{p.document.comment&&<section className="financePrintDetails"><p><span>Примітка</span><b>{p.document.comment}</b></p></section>}<section className="financePrintFooter"><div><span>Відповідальний за проведення</span><b>{p.document.postedBy||p.document.createdBy}</b></div><div><span>Підпис</span><i/></div></section><p className="financePrintVersion">Форма v{s.templateVersion} · snapshot #{s.id} · SHA-256 {s.sha256.slice(0,12)}…</p></article></main>}
