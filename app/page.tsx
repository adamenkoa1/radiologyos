import { redirect } from "next/navigation";

// Публічна головна — це теал-вітрина `/site/`. У продакшені воркер віддає її
// прямо на "/" (див. worker/index.ts), тож ця сторінка сюди зазвичай не
// доходить. Лишається лише як безпечний фолбек: якщо запит на "/" дійшов до
// Next в обхід воркера — відправляємо на статичну вітрину. Попередній
// React-лендинг прибрано як мертвий код (його ніхто не бачив).
export default function Home() {
  redirect("/site/");
}
